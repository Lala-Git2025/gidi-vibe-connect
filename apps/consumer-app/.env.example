# Crash reporting (optional). Leave empty to disable Sentry entirely.
# Inlined at bundle time: set it as an EAS environment variable (plain text,
# every build profile) for cloud builds, or copy this file to .env locally.
EXPO_PUBLIC_SENTRY_DSN=
